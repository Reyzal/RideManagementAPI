[project]
name = "ridemanagementapi"
version = "0.1.0"
description = "A query-efficient Django REST API case study for ride operations"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0.6",
    "django-filter>=25.2",
    "djangorestframework>=3.17.1",
    "djangorestframework-stubs>=3.17.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.0",
    "pytest-django>=4.12.0",
]
